// This file provides a default entry point when the raw leaflet repos
// is added directly to a project. In this configuration, you might
// want to enable webpack's ProvidePlugin, like so:
//
// new webpack.ProvidePlugin({L:'leaflet','window.L':'leaflet'})
//
export * from './src/Leaflet.js';
